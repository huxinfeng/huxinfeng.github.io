---
import Hr from './Hr.astro';
import Socials from './Socials.astro';

const CopyrightYear = `${new Date('2024.08.06 09:14:13').getFullYear()}-${new Date().getFullYear()}`;

export interface Props {
  noMarginTop?: boolean;
}

const { noMarginTop = false } = Astro.props;
---

<footer class={`${noMarginTop ? '' : 'mt-auto'}`}>
  <Hr noPadding />
  <div class='footer-wrapper'>
    <Socials centered />
    <div class='copyright-wrapper'>
      <span>Copyright &#169; {CopyrightYear}</span>
      <span class='separator'>&nbsp;|&nbsp;</span>
      <span>版权所有</span>
      <span class='separator'>&nbsp;|&nbsp;</span>
      <span>
        <a href='https://beian.miit.gov.cn' target='_blank' rel='nofollow'>京ICP备2023021201号-1</a>
      </span>
    </div>
  </div>
</footer>

<style>
  footer {
    @apply w-full;
  }

  .footer-wrapper {
    @apply flex flex-col items-center justify-between py-6 sm:flex-row-reverse sm:py-4;
  }

  .link-button {
    @apply my-1 p-2 hover:rotate-6;
  }

  .link-button svg {
    @apply scale-125;
  }

  .copyright-wrapper {
    @apply my-2 flex flex-col items-center whitespace-nowrap sm:flex-row;
  }

  .separator {
    @apply hidden sm:inline;
  }
</style>
